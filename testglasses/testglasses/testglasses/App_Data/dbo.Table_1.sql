CREATE TABLE [dbo].[seleciona]
(
	[cod_seleciona] INT NOT NULL PRIMARY KEY, 
    [favorito] BIT NOT NULL, 
    [comprado] BIT NOT NULL, 
    [cod_perfil] INT NOT NULL, 
    [cod_oculos] INT NOT NULL
)
